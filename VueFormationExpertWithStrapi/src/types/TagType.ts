// Modèle front utilisé par le store + lib
export type TagType = {
  id: string; // identifiant front (documentId)
  title: string;
  color: string;
  createdAt: string;
  updatedAt: string;
  publishedAt: string;
};
